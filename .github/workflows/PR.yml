name: PR

on:
  pull_request:
    paths-ignore:
      - trainings/**

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Unit Tests
        shell: pwsh
        run: ./tests/Invoke-Tests.ps1 -testTag @("UnitTests")